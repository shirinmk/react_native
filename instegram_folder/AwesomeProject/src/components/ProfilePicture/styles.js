import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    container:{
        //arround container is border
        margin:5,
        height:76,
        width:76,
        borderRadius:40, // make circle,
        borderWidth: 3,
        borderColor:"#666"
    },
    image:{
        height:70,
        width:70,
        borderRadius:40, // make circle,
        borderWidth: 1,
        borderColor:"#fff"
    }
});
export default styles;
